---
 kernel/time/tick-broadcast.c |    2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

Index: linux-2.6.24.7-rt21/kernel/time/tick-broadcast.c
===================================================================
--- linux-2.6.24.7-rt21.orig/kernel/time/tick-broadcast.c	2008-10-08 22:25:16.000000000 -0400
+++ linux-2.6.24.7-rt21/kernel/time/tick-broadcast.c	2008-10-08 22:25:17.000000000 -0400
@@ -498,7 +498,7 @@ static void tick_broadcast_init_next_eve
 	struct tick_device *td;
 	int cpu;
 
-	for_each_cpu_mask_nr(cpu, *mask) {
+	for_each_cpu_mask(cpu, *mask) {
 		td = &per_cpu(tick_cpu_device, cpu);
 		if (td->evtdev)
 			td->evtdev->next_event = expires;
