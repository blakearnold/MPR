---
 mm/highmem.c |    9 ---------
 1 file changed, 9 deletions(-)

Index: linux-2.6.24.2-rt2/mm/highmem.c
===================================================================
--- linux-2.6.24.2-rt2.orig/mm/highmem.c	2008-02-21 13:10:29.000000000 -0500
+++ linux-2.6.24.2-rt2/mm/highmem.c	2008-02-21 13:12:20.000000000 -0500
@@ -104,15 +104,6 @@ static void flush_all_zero_pkmaps(void)
 	flush_tlb_kernel_range(PKMAP_ADDR(0), PKMAP_ADDR(LAST_PKMAP));
 }
 
-/* Flush all unused kmap mappings in order to remove stray
-   mappings. */
-void kmap_flush_unused(void)
-{
-	spin_lock(&kmap_lock);
-	flush_all_zero_pkmaps();
-	spin_unlock(&kmap_lock);
-}
-
 static inline unsigned long map_new_virtual(struct page *page)
 {
 	unsigned long vaddr;
