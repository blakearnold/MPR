---
 kernel/sched_rt.c |    4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

Index: linux-2.6.24.7-rt21/kernel/sched_rt.c
===================================================================
--- linux-2.6.24.7-rt21.orig/kernel/sched_rt.c	2008-10-08 22:23:58.000000000 -0400
+++ linux-2.6.24.7-rt21/kernel/sched_rt.c	2008-10-08 22:23:59.000000000 -0400
@@ -684,10 +684,10 @@ static void post_schedule_rt(struct rq *
 	 * first via finish_lock_switch and then reaquire it here.
 	 */
 	if (unlikely(rq->rt.overloaded)) {
-		spin_lock_irq(&rq->lock);
+		spin_lock(&rq->lock);
 		push_rt_tasks(rq);
 		schedstat_inc(rq, rto_schedule_tail);
-		spin_unlock_irq(&rq->lock);
+		spin_unlock(&rq->lock);
 	}
 }
 
